- unless current_cart.empty?
  %section[current_cart]
    %h1= t('.cart.title')
    = render(current_cart)
    = render(@store.account)

%header
  %h1= t('.title')
  %p= t('.blurb.one')
  %p= t('.blurb.two')

= render(@store.products)
