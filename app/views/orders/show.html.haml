= render :partial => 'orders/summary', :locals => { :order => @order }

- if @order.payment
  = render :partial => @order.payment
- else
  = render :partial => @order.payment_methods, :locals => { :order => @order }
