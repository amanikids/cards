- unless current_cart.empty?
  %section[current_cart]
    %h1= t('.cart.title')
    = render(current_cart)
    = render(@store.account)

%header
  %h1= t('.title')
  = simple_format(@store.description)

= render(@store.products.available)
