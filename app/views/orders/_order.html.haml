%table
  %thead
    %tr
      %th
      %th
      %th Unit
      %th Qty
      %th Total
  %tbody
    - order.items.each do |item|
      %tr
        %td= item.product_name
        %td= item.variant_description
        %td= item.variant_price
        %td= item.quantity
        %td= item.total
  %tfoot
    %tr
      %td
      %td
      %td
      %td
      %td= order.total

- if order.new_record?
  %ul
    %li= link_to 'Update Quantities', edit_cart_path
