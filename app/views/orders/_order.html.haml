%table[order]
  %tbody
    - order.cart.items.each do |item|
      %tr
        %td= item.name
        %td= item.quantity
        %td= item.price
  %tfoot
    %tr
      %td{:colspan => 2}= t('.total')
      %td= order.cart.total
