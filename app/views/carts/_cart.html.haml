%table.cart
  %tbody
    - cart.items.each do |item|
      %tr
        %td= item.name
        %td= item.quantity
        %td= item.price
  %tfoot
    %tr
      %td{:colspan => 2}= t('.total')
      %td= cart.total
